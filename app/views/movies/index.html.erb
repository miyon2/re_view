<p id="category">
    영화리뷰
</p>

<hr>

<% @movies.each do |movie| %>
  <div class="row">
    <p class="col-md-1 catalogue catalogue-title">
      <%= movie.id %>
    </p>
    <p class="col catalogue">
      <%= link_to movie.title, movie, class: "col" %>
    </p>
  </div>
  <hr>
<% end %>

<br>

<div class="container row">
  <%= link_to '새 리뷰 작성하기', new_movie_path, style: "margin-top:5px;" %>

  <div class="ml-auto">
    <%= form_tag search_movies_path, method: :get, role: "search" do %>
      <div class="row">
        <%= text_field_tag :search, params[:search], class: "ml-auto", id: "search_form" %>
        <%= image_submit_tag("search.png", id:"search_image")%>
      </div>
    <% end %>
  </div>
</div>
